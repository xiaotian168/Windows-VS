#include<stdio.h>
#include<stdlib.h>

int main()
{
	/****************************************************************/
	/*
			写一个代码判断当前系统是32位操作系统还是64位操作系统。
	*/
	/****************************************************************/
	void *p = NULL;
	if (sizeof(p) == 4)
		printf("32位系统\n");
	else if (sizeof(p) == 8)
		printf("64位系统\n");
	system("pause");
	return 0;
}